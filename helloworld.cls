public with sharing class helloworld{
    public void showMessage(){
           system.debug('Hello World');
    }
    public integer addNumbers(integer firstNum, integer secondNum){
       addNumbers = (firstNum + secondNum);
       return addNumbers;
    }
   }
